spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:file:D:\projects\projects\src\main\resources\db_save/employee_DB_save
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update